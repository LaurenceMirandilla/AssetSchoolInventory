@model int

<a class="nav-link text-dark position-relative" asp-controller="Notifications" asp-action="Index">
    Notifications
    @if (Model > 0)
    {
        <span class="badge rounded-pill bg-danger ms-1">@(Model > 99 ? "99+" : Model.ToString())</span>
    }
</a>
